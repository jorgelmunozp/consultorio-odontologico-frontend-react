export const getTratamientosFiltered = ( array,code='',name='',consultoryRoom='',doctor='' ) => {
    return array.filter( item => item.id.toString().includes(code) && item.tratamiento.nombre.toLowerCase().includes(name.toLowerCase()) && item.tratamiento.consultorio.numero.toLowerCase().includes(consultoryRoom.toLowerCase()) && (item.tratamiento.doctor.nombre.toLowerCase().includes(doctor.toLowerCase()) || item.tratamiento.doctor.apellido.toLowerCase().includes(doctor.toLowerCase())) );
}